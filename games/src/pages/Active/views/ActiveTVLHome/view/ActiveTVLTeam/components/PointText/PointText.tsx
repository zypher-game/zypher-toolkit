import React, { memo } from 'react'
const PointText = memo(({ className }: { className: string }) => {
  return <p className={className}>Get more points and improve your ranking by staking more assets or inviting more friends!</p>
})
export default PointText
